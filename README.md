# haskat

GNU cat clone in Haskell.

## Build

```bash
cabal build
```

## Install

```bash
cabal install --installdir=$HOME/.local/bin
```

## Usage

```bash
haskat [OPTIONS] [FILE...]
```

Run `haskat --help` for options.
